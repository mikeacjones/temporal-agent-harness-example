apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent-harness-workspace-executor-testing
  namespace: temporal-michaelj-agent-harness-demo
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-harness-workspace-executor-testing
  namespace: temporal-michaelj-agent-harness-demo
  labels:
    app: agent-harness-workspace-executor-testing
    component: workspace-executor
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: agent-harness-workspace-executor-testing
      component: workspace-executor
  template:
    metadata:
      labels:
        app: agent-harness-workspace-executor-testing
        component: workspace-executor
    spec:
      serviceAccountName: agent-harness-workspace-executor-testing
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: Unconfined
      initContainers:
        - name: workspace-firewall
          image: 429214323166.dkr.ecr.us-west-1.amazonaws.com/temporal-michaelj-agent-harness-demo:testing
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -ec
            - |
              iptables -F OUTPUT
              iptables -P OUTPUT DROP
              iptables -A OUTPUT -o lo -j ACCEPT
              iptables -A OUTPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
              iptables -A OUTPUT -p udp --dport 53 -j ACCEPT
              iptables -A OUTPUT -p tcp --dport 53 -j ACCEPT
              for cidr in \
                0.0.0.0/8 10.0.0.0/8 100.64.0.0/10 127.0.0.0/8 \
                169.254.0.0/16 172.16.0.0/12 192.0.0.0/24 192.0.2.0/24 \
                192.168.0.0/16 198.18.0.0/15 198.51.100.0/24 \
                203.0.113.0/24 224.0.0.0/4 240.0.0.0/4; do
                iptables -A OUTPUT -d "${cidr}" -j DROP
              done
              iptables -A OUTPUT -p tcp -m multiport --dports 80,443 -j ACCEPT
              if ip6tables -L OUTPUT >/dev/null 2>&1; then
                ip6tables -F OUTPUT
                ip6tables -P OUTPUT DROP
                ip6tables -A OUTPUT -o lo -j ACCEPT
                ip6tables -A OUTPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
              fi
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
            runAsGroup: 0
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
              add:
                - NET_ADMIN
      containers:
        - name: workspace-executor
          image: 429214323166.dkr.ecr.us-west-1.amazonaws.com/temporal-michaelj-agent-harness-demo:testing
          imagePullPolicy: IfNotPresent
          command:
            - python
            - -m
            - simple_chat_agent.worker.sandbox.executor
          ports:
            - name: executor
              containerPort: 8082
          env:
            - name: WORKSPACE_EXECUTOR_TOKEN
              valueFrom:
                secretKeyRef:
                  name: agent-harness-workspace-executor-auth-testing
                  key: token
            - name: WORKSPACE_EXECUTOR_ROOT
              value: /workspaces
            - name: WORKSPACE_EXECUTOR_PORT
              value: "8082"
            - name: WORKSPACE_EXECUTOR_MAX_CONCURRENT
              value: "4"
            - name: PYTHONDONTWRITEBYTECODE
              value: "1"
          volumeMounts:
            - name: workspaces
              mountPath: /workspaces
            - name: executor-tmp
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /healthz
              port: executor
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: executor
            initialDelaySeconds: 10
            periodSeconds: 15
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1Gi
      volumes:
        - name: workspaces
          # The sa-demo cluster currently has no dynamic block-storage CSI
          # provisioner. This still persists files between tool calls for the
          # lifetime of the executor pod; production uses the PVC-backed form.
          emptyDir:
            sizeLimit: 5Gi
        - name: executor-tmp
          emptyDir:
            sizeLimit: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: agent-harness-workspace-executor-testing
  namespace: temporal-michaelj-agent-harness-demo
spec:
  selector:
    app: agent-harness-workspace-executor-testing
    component: workspace-executor
  ports:
    - name: executor
      port: 8082
      targetPort: executor
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: agent-harness-workspace-executor-testing
  namespace: temporal-michaelj-agent-harness-demo
spec:
  podSelector:
    matchLabels:
      app: agent-harness-workspace-executor-testing
      component: workspace-executor
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: agent-harness-testing
              component: worker
      ports:
        - protocol: TCP
          port: 8082
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
            except:
              - 0.0.0.0/8
              - 10.0.0.0/8
              - 100.64.0.0/10
              - 127.0.0.0/8
              - 169.254.0.0/16
              - 172.16.0.0/12
              - 192.0.0.0/24
              - 192.0.2.0/24
              - 192.168.0.0/16
              - 198.18.0.0/15
              - 198.51.100.0/24
              - 203.0.113.0/24
              - 224.0.0.0/4
              - 240.0.0.0/4
      ports:
        - protocol: TCP
          port: 80
        - protocol: TCP
          port: 443
